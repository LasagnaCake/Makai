import core.convert.tostring;

namespace IO {
	namespace Impl {
		func write(content: string): void {
			in print content;
		}
	}
	
	macro write(...content) -> {Impl.write(Convert.toString(#content))}

	macro writeLine(...content) -> {write(Convert.toString(#content), "\n")}
}