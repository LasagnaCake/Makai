using import core

enum Weekday: uint8 {
	MONDAY = 1
	TUESDAY
	WEDNESDAY
	THURSDAY
	FRIDAY
	SATURDAY
	SUNDAY
	
	fullName :: (self: Weekday) =>
		switch self {
			Weekday.MONDAY		=>	"Monday"
			Weekday.TUESDAY		=>	"Tuesday"
			Weekday.WEDNESDAY	=>	"Wednesday"
			Weekday.THURSDAY	=>	"Thursday"
			Weekday.FRIDAY		=>	"Friday"
			Weekday.SATURDAY	=>	"Saturday"
			Weekday.SUNDAY		=>	"Sunday"
			else				=>	"INVALID_DAY"
		}
	
	shortName :: (@ByCopy self: Weekday) =>
		match {
			self == Weekday.MONDAY		=>	"Mon"
			self == Weekday.TUESDAY		=>	"Tue"
			self == Weekday.WEDNESDAY	=>	"Wed"
			self == Weekday.THURSDAY	=>	"Thu"
			self == Weekday.FRIDAY		=>	"Fri"
			self == Weekday.SATURDAY	=>	"Sat"
			self == Weekday.SUNDAY		=>	"Sun"
			else						=>	"INVALID_DAY"
		}
	
	display :: (@ByRef self: Weekday) =>
		IO.writeLine(self.fullName; + " (" + self.shortName; + ")")
}

@Main
main :: () {
	// Full mode
	day := Weekday.MONDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.TUESDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.WEDNESDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.THURSDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.FRIDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.SATURDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
	day = Weekday.SUNDAY
	IO.writeLine(day.fullName;)
	IO.writeLine(day.shortName;)
}
