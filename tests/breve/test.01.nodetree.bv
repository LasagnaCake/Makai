import core.io

using core.io

module Test.Namespace {
	@Nullable
	struct MySpecialStruct {
		foo :: (var: int) -> int = {
			return var + var
		}

		bar :: (var: int) -> int = foo(var) * var
	}
}

main {
    writeLine("Hello, World!")
}
